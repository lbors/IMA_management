---
slice_id: Telemarketing
slice_part_id: slice-part-test-01
namespace: espaco-testes
service_info:
  - service:  
    apiVersion: v1
    kind: Service
    metadata:
      name: example-service
      labels:
        app: example-service
    spec:
      ports:
      - port: 80
        targetPort: 4000
      type: NodePort
      selector:
    app: example-pod